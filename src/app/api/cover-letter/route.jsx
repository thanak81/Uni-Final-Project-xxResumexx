const { PrismaClient } = require("@prisma/client");

const prisma = new PrismaClient();


export const GET = async ()=> {
    const getAllData = await prisma
}   

export const POST = async (request)=>{

}
